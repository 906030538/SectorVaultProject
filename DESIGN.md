# Sector Vault Project
内容去中心化储存的基于Git的无头CMS，使用Astro开发。

## 主站点
1. 支持不同git平台部署（GitHub、Atomgit、Gitee）。
2. 支持OAuth（GitHub Device Flow）或PAT（GitHub App）授权认证。
3. 使用@gitee/typescript-sdk-v5实现giteeAdapter。
4. 使用octokit实现githubAdapter。
5. 与索引数据共仓库，通过不同分支管理。
6. i18n支持，默认支持简体中文、繁体中文、英语、日语。
7. 颜色主题支持，默认白色主题，识别系统深色模式自动切换黑色主题。
8. 全局导航左侧为站点logo、投稿列表、专栏、讨论、常见问题、网站介绍，右侧为投稿按钮、语言选择、主题选择、用户头像和用户名。
9. 未登陆时，点击导航栏的投稿或登陆按钮，弹出授权指引，选择数据托管平台，并引导用户完成注册和授权本站。

前端路由：
```
/ -- 首页
/project -- 投稿列表
/article -- 专栏列表
/discussions -- 讨论
/faq -- 常见问题
/about -- 站点介绍
/user/:name -- 用户空间
/view/:name/:repo -- 集合详情
/view/:name/:repo/:slug -- 详情页
/edit/:name/:repo/:slug -- 编辑稿件
/new -- 新建投稿
```

### 投稿列表
1. 从索引仓加载，分页展示最近的投稿，默认展示10条。
2. 点击下一页时，如果默认索引显示完毕，则自动加载已归档的索引，直至遍历完所有索引，缓存已加载的索引。加载期间在列表最下面显示加载中图标和取消按钮。
3. 提供筛选器，可以根据关联曲目、合成引擎、使用声库、歌曲语言筛选，样式为可自定义输入的下拉框，点击搜索时才生效。
4. 提供搜索器，输入关键字点搜索按钮后，遍历索引，列出关联稿件。
5. 列表显示稿件封面、标题、投稿时间、用户名、关联曲目、合成引擎、使用声库、歌曲语言、有无参数。
6. 自动获取当前页的稿件关联的issue和release，获取评论数和点赞数，并缓存。

### 投稿详情
1. 页面开始展示投稿标题和时间，以列表形式显示关联曲目、合成引擎、使用声库、歌曲语言、有无参数。
2. 如果有，显示关联视频链接。
3. 然后渲染正文，不用包含文件头的各种属性。
4. 正文结束后以横排按钮形式显示tag列表。
5. 然后显示仓库内媒体，图片提供预览，音频、视频提供播放组件。
6. 显示投稿用户名、头像、仓库名，仓库收藏（star）数，和当前稿件许可证名称，并能够点击查看跳转；当前稿件没有指定许可证时显示仓库的许可证。
7. 然后列出工程文件，如果有加密提供密码输入框，点击下载在前端先加载完整个文件，先解压解密再返回浏览器文件。
8. 嵌入稿件关联的release并展示互动记录和附件列表。
9. 嵌入稿件关联的issue列表当作留言。

### 专栏列表
1. 基本与投稿列表一致，但不提供筛选器。
2. 稿件只显示标题、投稿时间、用户名，如果有封面或任意图片媒体也显示。

### 专栏详情
1. 页面开始展示投稿标题、时间和用户名、头像、仓库名。
2. 然后渲染正文，不用包含文件头的各种属性。
3. 嵌入稿件关联的issue列表当作留言。

### 讨论
1. 显示主站点仓库的discussions页面作为讨论区。

### 场景问题
1. 提供页面显示主站点仓库的wiki页面作为FAQ。

### 站点介绍
1. 介绍本网站架构和优势。
2. 显示索引的稿件数量和用户数量。

## 用户空间
用户空间既作为主站点的一个子路由，也可以独立部署；用户可自行部署用户空间作为内容仓的静态页面，只展示当前仓库的数据。

用户空间独立部署时路由：
```
/ -- 用户空间
/view/:repo -- 当前集合详情
/view/:repo/:slug -- 详情页
/edit/:repo/:slug -- 编辑稿件
/new -- 新建投稿
```

### 个人首页
1. 显示用户名头像。主站点部署时，如果当前用户索引包含静态部署页面，提供按钮跳转。
2. 如果已经登录的账户是当前用户，显示新建集合按钮，在弹出界面引导用户创建新仓库。
   1. 仓库名输入框分前置和名称两部分，前缀默认为`svp-`，可通过部署配置默认前缀。
   2. 右侧下拉框选择仓库创建的属主，可能是当前账户或账户所在组织。
   3. 提供模板库选择，具体哪些平台有哪些模板库通过主站点静态部署配置。
   4. 提供下拉框选择默认许可证，提供CC0、CC4.0（含细分系列）可选，可以为空。
3. 主站点部署时，展示当前用户下所有固定前缀的仓库作为合集，并显示每个仓库最近的3个工程稿件封面标题日期和更多按钮，合集之间水平分隔。显示仓库收藏（star）数量。
4. 将包含专栏投稿的仓库再显示一遍，显示最近的3个专栏稿件和更多按钮，合集之间水平分隔。
5. 工程和专栏合集有大标题分隔。
6. 如果某个仓库包含个人介绍文件（`ABOUT.md`），展示第一个找到的内容？
7. 主站点部署时，如果索引包含同名但不同托管平台的用户及仓库时，通过一个查询参数`git`区分展示的数据。

### 集合详情
1. 个人页点击集合更多按钮，或投稿详情页点击仓库名进入集合详情页。
2. 显示当前仓库名、用户名和头像、仓库许可证。
3. 如果已经登录的账户是当前用户，显示投稿、编辑和删除按钮。
   1. 投稿按钮跳转新建投稿页面。
   2. 编辑按钮弹出编辑框，提供许可证选择框，和保存按钮。
   3. 删除按钮弹出警告窗口，提示输入当前仓库名，再次确认后删除仓库。
4. 工程和专栏各一个标签按钮，切换显示。
5. 工程标签分页展示当前仓库内所有工程稿件，默认10稿每页，显示封面标题日期。如果已经登录的账户是当前用户，稿件上显示编辑和删除按钮。
6. 自动获取当前页的稿件关联的issue和release，获取评论数和点赞数，并缓存。
7. 专栏标签分页展示当前仓库内所有专栏稿件。

### 编辑器
1. 新建投稿时，显示投稿仓库下拉框、slug输入框和工程、专栏互斥按钮；slug的placeholder显示为当前6位数字日期+标题，如果用户修改则使用用户输入内容；编辑稿件时，这两个框不可编辑。
2. 标题输入框独占一整行。
3. 提供有参、微调、无参数单选框；视频站链接、关联曲目、合成引擎、使用声库、歌曲语言都提供一个列表输入框，通过加减按钮增加删除填写多个值，每个类型最多10个值；
4. 提供封面图片上传组件。
5. 正文输入框提供可视化Markdown编辑器。
6. 提供标签输入框，输入一个标签按回车生成标签组件带删除按钮，最多10个标签。
7. 提供下拉框选择许可证，默认留空即与仓库级许可相同。
8. 提供工程文件上传控件，也可以上传其他媒体；每个文件提供一个下拉框，控制上传方案；选中且文件内容是json或xml格式则默认选择格式化，其他类型文件默认选择不处理；提供删除按钮。提示文件过大或可能违反许可证的资源通过附件上传。
   1. 不处理: 将文件原始字节上传到仓库。
   2. 格式化: 将json或xml格式文件折行缩进再上传；
   3. 压缩: 使用ZIP将文件压缩一遍再上传；
   4. 加密: 额外显示密码输入框，使用ZIP压缩加密文件后上传。
9. 提供发布简介输入框，默认单行显示，但允许输入多行。
10. 提供附件上传控件，每个文件提供删除按钮。
11. 新建投稿流程：
    1. 先创建标题同名issue，记录issue编号。
    2. 然后将封面、工程文件上传控件的所有文件都写入slug目录。
    3. 然后写入正文`README.md`到slug目录，头部固定包含`*Powered by Sector Vault Project*`，然后写入投稿属性，包含issue编号和封面文件名，插入分隔符；然后写入正文文本，再插入分隔符；最后插入列表包含工程文件上传控件的所有文件，压缩或加密文件额外在文件名前加一个或两个`*`号。
    4. 添加当前slug名+slug相对路径的链接到本仓库的`README.md`的列表。
    5. 添加索引到本仓库的`svp-archive.json`，只记录`submissions`。
    6. 然后对最新的提交创建slug同名release，带上发布简介输入框内容，包含主站点当前投稿的详情页面链接，如果当前站点有部署静态页面也显示对应的链接。
    7. 然后将附件上传控件的所有文件上传到这个release的附件。
    8. 最后向索引仓发送一个轻量的pr，修改未归档的索引，包含当前稿件的索引的必要内容。
12. 修改投稿流程：
    1. 如果封面有修改，则删掉旧封面文件，然后将新封面写入slug目录。
    2. 如果工程文件上传控件有修改，删掉不要的旧文件，然后写入新文件。
    3. 按照投稿时的规则根据修改后的正文和文件列表重新生成文本写入`README.md`。
    4. 如果附件上传控件有修改，也同步到关联的release。
    5. 如果封面标题或其他索引属性有修改，则更新本仓库的`svp-archive.json`，并向索引仓发送一个轻量的pr，包含当前稿件最新的索引，投稿时间不变更。

## 主站点-索引仓
1. 只包含一系列json和必要的配置文件。
2. 稿件索引记录每个稿件的投稿用户、标题、封面链接、投稿时间、发布时间、关联曲目、合成引擎、使用声库、歌曲语言、有无参数；在同一个索引文件中记录关联的用户，记录用户所在平台、投稿的仓库、静态页面链接，每个仓库一条记录。
3. 索引按照月份归档，保存到`index/archive/YYYY-MM.json`，每个投稿按投稿时间确定保存的索引文件。
4. 定时任务检查用户索引里的仓库是否存在，不存在则应删除这条索引。
5. 定时任务生成`index/current.json`，记录所有归档的文件名和其投稿数量，分类型统计；另外记录最近的投稿及用户，数量由配置文件控制。
6. 索引分支允许所有人提交pr，门禁校验通过后自动合入。

`current.json`内容参考如下，另外归档json结构类似但只有`submissions`和`users`字段：
```json
{
   "archives": [{
      "file": "2026-09.json",
      "byType": {
         "project": 1,
         "article": 0,
      }
   }],
   "users": 1,
   "submissions": [{
      "platform": "github",
      "owner": "testUser",
      "slug": "260901-test",
      "title": "test",
      "cover": "./cover.png",
      "submittedAt": "2026-09-01T12:00:00Z",
      "publishedAt": "2026-09-01T12:00:00Z",
      "type": "project",
      "paramState": "with-param",
      "song": ["My song"],
      "engines": ["Vocaloid", "Synthesizer V"],
      "voicebanks": ["Miku", "Teto"],
      "languages": ["jp"]
   }],
   "users": [{
      "platform": "github",
      "owner": "testUser",
      "displayName": "测试",
      "avatar": "https://...",
      "pagesUrl": "https://...",
      "repod": [{
         "repo": "svp-default"
      }]
   }]
}
```

## 内容仓
```
public/ -- 静态目录预留
public/admin/ -- DecapCMS 兼容目录
public/admin/config.yml -- DecapCMS 配置文件
posts/ -- 数据目录
posts/[slug] -- 投稿目录
posts/[slug]/README.md -- 正文
posts/[slug]/cover.png -- 封面
posts/[slug]/project.vsqx -- 工程文件
posts/[slug]/... -- 其他內联媒体
README.md -- 简单目录
svp-archive.json --  本地索引
```

1. 内容仓库结构兼容DecapCMS。
2. 媒体不统一放一个目录，而是与稿件相同目录。
3. 支持用户自行部署静态页面。
4. 生成RSS兼容的`atom.xml`
