---
import BaseLayout from '@/layouts/BaseLayout.astro';
import UserRoute from '@/components/routes/UserRoute.astro';

/**
 * 动态路由 /user/:name。
 * 不做构建期预渲染（避免按用户生成具体页面），全部由 404 SPA 回退客户端渲染。
 */
export function getStaticPaths() {
  return [];
}
---

<BaseLayout>
  <UserRoute />
</BaseLayout>
